# 🎮 CrossVerse Arena —— AI 代码生成指令文档（生产级画质增强版）

**项目名称** 宇宙竞技场(CrossVerse Arena) 

**项目目标** 构建一个高度模块化、完全由配置驱动的跨 IP 角色对战塔防平台，支持任意游戏角色作为攻防双方，无需修改代码即可扩展新内容，同时具备生产级画质表现、流畅打击感与稳定性能。

**项目参考** 植物大战僵尸（核心玩法框架）+ 王者荣耀（打击感与画质基准）+ 我的世界（模块化资源管理）

## 🔧 一、总体要求（生产级增强版）

**核心原则**：

- •**绝对零硬编码**：禁止在代码中出现任何具体游戏IP名称、角色名、技能名、数值常量、画质参数、模型路径
- •**全配置驱动**：所有内容（玩法、角色、技能、画质、性能参数）必须通过 `YAML`配置文件定义，资源路径通过相对路径引用
- •**企业级架构**：采用模块化设计、依赖注入、服务定位器模式，支持热更新与灰度发布
- •**中文注释**：所有关键类、方法、逻辑分支、配置字段必须包含 **详细中文注释**，注明用途、约束条件与默认值
- •**扩展预留**：所有模块必须预留扩展接口，支持通过脚本/配置扩展功能（含渲染管线、打击反馈、性能优化策略）
- •**管理界面驱动**：所有配置操作（含画质参数、模型上传、性能阈值设置）必须通过 **管理界面** 完成，禁止手动创建或修改文件
- •**安全审计机制**：所有操作记录日志；删除操作进入回收站，可恢复；关键配置修改需二次确认
- •**生产级画质基准**：图形渲染达到主流手游画质标准，支持动态光影、粒子特效分层、模型LOD优化、抗锯齿等核心功能
- •**流畅打击感**：通过帧同步、碰撞反馈、音效振动联动、伤害数值飘出优化，实现清晰可感知的打击体验
- •**性能稳定性**：内置性能监控与自适应调节机制，保证主流设备帧率稳定≥30FPS，峰值内存占用不超过设备可用内存的60%

> 💡 **设计哲学**：
>
> "我们不是在编写游戏，而是在构建**配置驱动的生态系统**。
>
> 管理界面不是工具，而是**唯一正确的配置入口**。
>
> 任何手动操作都是对架构的背叛。
>
> 画质与性能不是对立项，而是通过配置协同优化的核心体验。
> 这是一个平台。它应该能容纳任何IP的角色，通过配置实现一切。"

------

## 📂 二、项目目录结构（生产级增强版）

```
CrossVerseArena/
├── core/                      # 核心系统模块
│   ├── game_engine.py        # 游戏主循环与状态机（含帧同步控制）
│   ├── entity_manager.py     # 实体管理器（角色/技能/子弹/模型实例）
│   ├── config_loader.py      # 配置加载器（支持 YAML 自动扫描 + 热加载）
│   ├── resource_loader.py    # 资源加载器（图片/音频/特效/模型，支持预加载）
│   ├── skill_executor.py     # 技能执行引擎（支持多线程/协程，含打击反馈调度）
│   ├── save_system.py        # 存档系统（JSON + 版本控制）
│   ├── skin_manager.py       # 皮肤管理系统（含模型材质切换）
│   ├── effect_manager.py     # 特效效果管理器（含粒子分层渲染）
│   ├── admin_manager.py      # 管理界面核心引擎
│   ├── render_manager.py     # 图形渲染管理器（新增，控制渲染管线）
│   ├── performance_monitor.py# 性能监控系统（新增，帧率/内存/CPU占用监控）
│   ├── hit_feedback_manager.py # 打击感管理器（新增，音效/振动/视觉反馈联动）
│   └── model_manager.py      # 模型资源管理器（新增，LOD控制/骨骼动画加载）
│
├── admin/                     # 管理界面模块（仅开发人员可见）
│   ├── admin_ui.py           # 管理界面UI实现（新增画质/模型/性能配置面板）
│   ├── admin_manager.py      # 管理逻辑核心（与core/admin_manager.py协同）
│   ├── asset_uploader.py     # 素材上传管理器（支持模型/材质/特效文件上传）
│   ├── config_schema/        # 配置验证Schema
│   │   ├── game_meta_schema.yaml    # 游戏IP元信息Schema
│   │   ├── character_schema.yaml    # 角色配置Schema（含模型/画质相关字段）
│   │   ├── skill_schema.yaml        # 技能配置Schema（含打击反馈字段）
│   │   ├── skin_schema.yaml         # 皮肤配置Schema（含材质/模型变体）
│   │   ├── campaign_schema.yaml     # 战役元数据Schema
│   │   ├── level_schema.yaml        # 关卡配置Schema（含地形/光影配置）
│   │   ├── effect_schema.yaml       # 技能效果配置Schema
│   │   ├── render_schema.yaml       # 渲染参数配置Schema（新增）
│   │   ├── model_schema.yaml        # 模型资源配置Schema（新增）
│   │   ├── performance_schema.yaml  # 性能监控配置Schema（新增）
│   │   └── hit_feedback_schema.yaml # 打击感配置Schema（新增）
│   ├── recycle_bin/          # 回收站（所有删除内容）
│   │   ├── config/           # 删除的配置文件
│   │   └── assets/           # 删除的资源文件（含模型/材质）
│   └── performance_dashboard.py # 性能监控面板（新增，实时查看设备运行数据）
│
├── modules/                  # 功能模块
│   ├── ui/
│   │   ├── menu_ui.py        # 主菜单/战役选择/关卡选择
│   │   ├── battle_hud.py     # 战斗HUD（资源栏/技能栏/状态显示/帧率显示）
│   │   ├── skin_selector.py  # 皮肤选择器（含3D模型预览）
│   │   ├── tooltip.py        # 悬浮提示（角色/技能详情）
│   │   ├── graphics_settings_ui.py # 画质设置界面（新增，玩家可调整参数）
│   │   └── performance_overlay.py # 性能悬浮窗（新增，显示帧率/内存占用）
│   ├── audio/
│   │   └── audio_manager.py  # 音频管理（背景音乐/音效/语音/打击音效分层）
│   ├── effects/
│   │   └── effect_system.py  # 特效系统（粒子/动画/光影/后处理效果）
│   ├── ai/
│   │   └── behavior_tree.py  # AI决策树（预留扩展点）
│   ├── graphics/             # 图形渲染模块（新增）
│   │   ├── render_pipeline.py # 渲染管线（Forward/Deferred可选）
│   │   ├── lighting_manager.py # 光影管理器（动态光/环境光/阴影控制）
│   │   ├── particle_system.py # 粒子系统（分层渲染/生命周期管理）
│   │   ├── post_processing.py # 后处理系统（抗锯齿/ Bloom/色调映射）
│   │   └── lod_manager.py    # LOD管理器（模型细节层级自动切换）
│   ├── physics/              # 物理系统模块（新增）
│   │   └── collision_manager.py # 碰撞检测管理器（精确碰撞盒/触发判定）
│   └── vibration/            # 振动反馈模块（新增）
│       └── vibration_manager.py # 振动控制（设备适配/强度/时长配置）
│
├── games/                    # 游戏IP资源目录（核心扩展点）
│   ├── dnf/
│   │   ├── meta.yaml         # 游戏元信息
│   │   ├── characters/       # 角色配置
│   │   │   ├── zhaohuan.yaml # 召唤师配置（含模型/打击反馈参数）
│   │   │   └── guijian.yaml  # 鬼剑士配置
│   │   ├── skins/           # 皮肤配置目录
│   │   │   ├── ice_summoner.yaml  # 冰晶召唤师皮肤（含材质/模型变体）
│   │   │   └── fire_guijian.yaml # 火焰鬼剑士皮肤
│   │   ├── models/           # 模型资源目录（新增）
│   │   │   ├── characters/   # 角色模型（FBX/GLB格式）
│   │   │   ├── weapons/      # 武器模型（新增）
│   │   │   └── props/        # 道具模型（新增）
│   │   ├── materials/        # 材质资源目录（新增，PBR材质）
│   │   └── assets/           # 基础资源目录
│   │       ├── sprites/      # 精灵图（PNG，高分辨率适配）
│   │       ├── sounds/       # 音效（WAV/OGG，含3D音效）
│   │       ├── skills/       # 技能特效（GIF/JSON/粒子配置）
│   │       ├── skins/        # 皮肤专属资源
│   │       ├── shaders/      # 专属着色器（新增）
│   │       └── post_process/ # 专属后处理配置（新增）
│   │
│   └── league_of_legends/    # 示例：英雄联盟
│       ├── meta.yaml
│       ├── characters/
│       │   ├── garen.yaml
│       │   └── lux.yaml
│       ├── skins/
│       │   ├── steel_legion_garen.yaml
│       │   └── elementalist_lux.yaml
│       ├── models/
│       │   ├── characters/
│       │   ├── weapons/
│       │   └── props/
│       ├── materials/
│       └── assets/
│
├── campaigns/                # 战役配置目录（核心扩展点）
│   ├── dnf_vs_lol/
│   │   ├── campaign.yaml     # 战役元数据
│   │   ├── levels/           # 多关卡配置
│   │   │   ├── level_01.yaml
│   │   │   └── boss_level.yaml
│   │   └── maps/             # 地图资源（增强）
│   │       ├── background.png # 背景图（4K分辨率适配）
│   │       ├── terrain.glb   # 3D地形模型（新增）
│   │       ├── path_points.json # 路径点配置
│   │       ├── placement_mask.png # 放置掩码（高精度，支持半透明区域判定）
│   │       ├── lighting.yaml # 关卡光影配置（新增）
│   │       └── collision_mask.png # 碰撞掩码（新增，精确碰撞区域）
│   │
│   └── lol_vs_genshin/
│
├── saves/                    # 存档目录
│   └── default_save.json
│
├── lang/                     # 多语言支持(优先中文, 其他语言留有扩展)
│   ├── zh.yaml
│   └── en.yaml
├── shaders/                  # 全局着色器目录（新增）
│   ├── standard_pbr.glsl     # 标准PBR着色器
│   ├── unlit.glsl            # 无光照着色器
│   ├── skill_effect.glsl     # 技能特效着色器
│   └── post_process.glsl     # 后处理着色器
├── performance/              # 性能配置目录（新增）
│   ├── device_profiles/      # 设备性能配置文件
│   │   ├── high_end.yaml     # 高端设备配置
│   │   ├── mid_end.yaml      # 中端设备配置
│   │   └── low_end.yaml      # 低端设备配置
│   └── default_thresholds.yaml # 性能阈值默认配置
├── settings.yaml             # 全局设置（含管理界面、画质、性能配置）
├── main.py                   # 程序入口（支持快捷键打开管理界面/性能面板）
└── README.md                 # 项目说明（含管理界面使用指南、画质优化手册）
```

------

## 🧱 三、核心模块功能说明（生产级增强版）

### 1. `core/config_loader.py`（增强）

- •自动扫描机制 ： 
  - 每隔30秒扫描 `games/`、`campaigns/`、`performance/`目录，自动注册新内容
  - 支持热加载：无需重启即可添加新角色/战役/皮肤/画质配置/性能策略
- •配置校验 ： 
  - 必须字段校验（如 `name`, `type`, `model_path`）
  - 路径合法性校验（相对路径转绝对路径，支持跨平台适配）
  - 依赖关系校验（技能必须引用存在的资源，模型必须关联对应的材质）
  - 数值范围校验（画质参数、性能阈值需符合Schema约束）
- •错误处理 ： 
  - 缺失字段：记录日志并跳过该配置，界面提示具体缺失项
  - 资源缺失：使用默认占位资源（模型用灰色方块，材质用纯色，音效用提示音）
  - 格式错误：拒绝加载并记录详细错误信息，支持一键修复基础格式问题

### 2. `core/skill_executor.py`（生产级增强）

- •技能执行引擎 ： 
  - 支持多线程/协程执行技能逻辑，保证技能释放不阻塞主循环
  - 冷却时间管理（全局冷却/技能单独冷却，支持配置冷却加速系数）
  - 技能释放条件判断（基于距离/血量/CD/地形阻挡）
  - 帧同步控制：技能生效时间精确到帧，保证多设备表现一致
- •技能类型扩展 ： 
  ```
  skill_type:
    ◦ projectile  # 弹道攻击（支持轨迹渲染/碰撞检测精度配置）
    ◦ aoe         # 范围伤害（支持范围可视化/分层特效）
    ◦ summon      # 召唤物（支持召唤物模型/AI配置）
    ◦ buff/debuff # 增益/减益（支持效果可视化图标）
    ◦ heal        # 治疗（支持治疗数值飘出/绿色反馈）
    ◦ control     # 控制效果（眩晕/击飞等，支持状态锁定）
    ◦ dot         # 持续伤害（支持伤害跳字/特效叠加）
    ◦ multi_hit   # 多段攻击（支持每段打击独立反馈）
    ◦ custom      # 自定义脚本（预留）
  ```
- •打击感联动 ： 
  - 技能命中时自动触发打击反馈（音效+振动+视觉特效+数值飘出）
  - 支持配置打击反馈强度（轻击/重击/暴击区分）
  - 多段攻击支持逐段增强反馈，提升连击爽感

### 3. `core/entity_manager.py`（增强）

- •实体生命周期管理 ： 
  - 创建：根据配置动态实例化角色/技能/子弹，自动加载对应的模型、材质、动画
  - 更新：每帧调用 `update()` 方法，同步位置、状态、动画播放进度、LOD等级
  - 销毁：自动回收资源（对象池优化），释放模型实例、粒子系统占用的内存
- •AI决策树接口 ： 
  - 预留 `on_attack()`、`on_hit()`、`on_move()` 等回调函数
  - 支持通过配置指定AI策略（如追击/逃跑/防御/绕路）
  - 支持基于地形的AI路径规划（结合地图碰撞掩码）
- •模型实例管理 ： 
  - 自动关联角色配置中的模型文件，支持骨骼动画播放与切换
  - 实时同步角色状态与模型动画（攻击/受伤/死亡动画自动触发）
  - 支持模型部件隐藏/显示（如武器切换、特效挂载）

### 4. `core/skin_manager.py`（生产级增强）

**核心职责**：

- •管理角色皮肤系统，支持动态切换皮肤（含模型变体、材质替换、特效叠加）
- •处理皮肤专属资源（模型、材质、着色器、动画）的加载与卸载
- •实现皮肤效果叠加和组合逻辑，保证画质一致性
- •关联皮肤与性能配置，高特效皮肤可自动适配设备性能

**关键功能**：

- •皮肤继承机制：皮肤继承基础角色属性、模型、动画，可覆盖特定字段
- •多皮肤管理：一个角色支持多个皮肤，每个皮肤有独立资源配置和画质参数
- •特效叠加：皮肤特效与技能特效分层渲染，避免画面混乱
- •皮肤解锁条件：支持通过配置定义皮肤解锁条件（等级/成就/购买）
- •性能适配：高画质皮肤可配置降级策略（低端设备自动关闭部分特效）

### 5. `core/effect_manager.py`（生产级增强）

**核心职责**：

- •管理技能效果的状态、持续时间、渲染层级
- •处理效果叠加、冲突解决、免疫机制
- •实现复杂的效果交互逻辑，保证特效渲染不影响帧率
- •支持特效分层（前景/中景/背景），提升画面层次感

**效果系统特性**：

- •持续时间管理：支持瞬时效果和持续效果，持续效果支持暂停/恢复
- •效果叠加规则：定义同类型效果叠加方式（覆盖、叠加、取最高等）
- •效果冲突解决：处理互斥效果逻辑（如加速与减速不能同时生效）
- •状态免疫机制：支持角色对特定效果的免疫，免疫时显示免疫图标
- •特效性能控制：支持配置特效粒子数量上限、渲染距离、LOD等级
- •效果同步：保证多设备上的效果显示、持续时间完全一致

### 6. `core/admin_manager.py`（生产级增强）

**核心职责**：

- •管理界面操作的**唯一入口**，所有配置操作（含画质、模型、性能）必须通过此模块执行
- •实现**配置验证**、**引用检查**、**回收站管理**、**性能阈值校验**
- •提供**操作日志**记录（`admin/admin_log.txt`），支持日志导出与筛选
- •管理模型、材质、特效等大文件的上传、存储、版本控制

**关键功能表**：

| 操作类型         | 功能说明                                  | 验证机制                                  | 回收站处理                          |
| ---------------- | ----------------------------------------- | ----------------------------------------- | ----------------------------------- |
| 新增游戏IP       | 创建新游戏目录结构（含models/materials）   | 名称唯一性、路径合法性                    | 无                                  |
| 新增角色         | 生成角色配置+资源目录+模型关联            | 游戏IP存在性、模型路径有效性              | 无                                  |
| 新增皮肤         | 生成皮肤配置+上传素材（模型/材质/特效）    | 角色存在性、材质与模型兼容性              | 无                                  |
| 新增技能         | 生成技能配置+上传素材+打击反馈配置        | 技能类型合法性、特效资源存在性            | 无                                  |
| 新增模型         | 上传模型文件（FBX/GLB）+ 配置关联          | 模型格式合法性、骨骼动画完整性            | 无                                  |
| 新增画质配置     | 创建渲染参数模板（光影/粒子/后处理）      | 参数范围合法性、设备适配性                | 无                                  |
| 删除角色         | 检查战役引用 → 移动到回收站               | 是否被战役引用、是否有依赖皮肤/技能       | 移动到 `recycle_bin/config`         |
| 删除技能         | 检查角色引用 → 移动到回收站               | 是否被角色使用、是否有依赖特效            | 移动到 `recycle_bin/config`         |
| 删除皮肤         | 检查角色引用 → 移动到回收站               | 是否被角色使用、是否有依赖资源            | 移动到 `recycle_bin/config`         |
| 删除模型/材质    | 检查配置引用 → 移动到回收站               | 是否被角色/皮肤/技能引用                  | 移动到 `recycle_bin/assets`         |
| 删除画质配置     | 检查是否为默认配置 → 移动到回收站         | 是否被关卡/角色引用                      | 移动到 `recycle_bin/config`         |
| 恢复项目         | 从回收站移回原位置                        | 路径有效性、目标位置无冲突                | 移回原位置                          |

**素材上传功能**（增强）：

- •支持批量上传素材文件（含模型、材质、粒子特效、着色器）
- •自动文件类型验证（模型：FBX/GLB，图片：PNG/JPG，音频：WAV/OGG，视频：MP4）
- •文件大小限制检查（可配置，如模型≤50MB，材质≤10MB）
- •自动重命名和路径分配，避免文件名冲突
- •重复文件检测和冲突解决（跳过/覆盖/重命名选项）
- •模型自动预处理：生成LOD层级、优化骨骼动画、转换纹理格式
- •材质自动校验：检查PBR材质参数完整性（基础色/金属度/粗糙度/法线贴图）

### 7. `core/render_manager.py`（新增核心模块）

**核心职责**：

- •统一管理游戏渲染管线，支持Forward/Deferred渲染切换
- •控制全局画质参数，实现画质等级自动适配
- •管理光影、粒子、后处理效果的渲染顺序与资源分配
- •优化渲染性能，减少Draw Call，避免画面卡顿

**关键功能**：

- •渲染管线配置：支持通过配置选择渲染管线，适配不同性能设备
- •画质等级管理：预设高/中/低三档画质模板，支持自定义参数
- •全局光影控制：统一管理环境光、方向光、点光源，支持动态光照强度调整
- •抗锯齿支持：支持FXAA/TAA抗锯齿算法，可配置抗锯齿强度
- •Draw Call优化：实现模型实例化渲染、批处理渲染，减少CPU开销
- •渲染排序：按图层（背景→地形→角色→技能→UI）排序，保证画面正确叠加
- •性能自适应：实时监控帧率，当帧率低于阈值时自动降低画质（如减少粒子数量、降低LOD等级）

### 8. `core/performance_monitor.py`（新增核心模块）

**核心职责**：

- •实时监控游戏运行状态（帧率、CPU占用、内存占用、Draw Call数量）
- •检测性能异常（帧率骤降、内存泄漏、CPU过载），触发告警与自适应调整
- •记录性能数据，生成性能报告，支持离线分析
- •管理设备性能配置文件，实现设备分级适配

**关键功能**：

- •实时监控指标：
  - 帧率（FPS）：每帧更新，记录平均帧率、最低帧率、帧率波动
  - 内存占用：监控物理内存、虚拟内存使用量，检测内存泄漏
  - CPU占用：监控各核心CPU使用率，定位高负载模块
  - 渲染性能：Draw Call数量、三角面数量、粒子数量、纹理内存占用
- •性能阈值配置：支持设置各指标的警告阈值、危险阈值（如帧率<30FPS触发警告）
- •自适应调整策略：
  - 帧率过低：降低粒子数量→降低模型LOD等级→关闭后处理效果→降低分辨率
  - 内存过高：释放未使用的模型/材质→减少缓存资源数量→降低纹理分辨率
- •性能日志：记录性能异常时间点、触发的调整策略、调整后的效果
- •设备分级：自动识别设备性能等级（高/中/低），加载对应的性能配置文件

### 9. `core/hit_feedback_manager.py`（新增核心模块）

**核心职责**：

- •统一管理技能命中、角色受伤、暴击等场景的反馈效果
- •实现音效、振动、视觉特效、数值飘出的联动，提升打击感
- •支持反馈强度配置，适配不同设备（如无振动功能的设备自动关闭振动）

**关键功能**：

- •反馈类型分类：
  - 轻击反馈：弱音效+轻微振动+白色小数值飘出
  - 重击反馈：强音效+中等振动+黄色大数值飘出+屏幕轻微抖动
  - 暴击反馈：特殊音效+强振动+红色发光数值飘出+屏幕闪白
  - 治疗反馈：治愈音效+无振动+绿色数值飘出+光晕特效
  - 控制反馈：控制音效+中等振动+状态图标弹出+屏幕边缘提示
- •反馈联动控制：保证音效、振动、视觉效果的触发时机精确同步（误差≤1帧）
- •设备适配：自动检测设备是否支持振动，支持配置振动强度（0-100%）
- •自定义反馈：支持通过配置定义新的反馈类型，关联对应的音效、振动、特效

### 10. `core/model_manager.py`（新增核心模块）

**核心职责**：

- •管理所有3D模型资源的加载、卸载、缓存
- •控制模型LOD（Level of Detail）等级切换，平衡画质与性能
- •处理模型骨骼动画、材质替换、部件挂载
- •保证模型渲染兼容性，避免出现纹理错乱、动画异常

**关键功能**：

- •模型格式支持：支持FBX、GLB格式，自动解析模型结构（网格、骨骼、动画）
- •LOD管理：
  - 自动生成LOD层级（高/中/低三档，可配置三角面数量比例）
  - 基于角色与相机的距离自动切换LOD等级（远距离用低模，近距离用高模）
  - 支持配置LOD切换距离阈值，避免切换过于频繁
- •动画管理：
  - 支持骨骼动画播放、暂停、循环、过渡
  - 自动关联角色状态与动画（攻击→攻击动画，受伤→受伤动画）
  - 支持动画混合（如移动时同时播放攻击动画）
- •材质管理：
  - 支持PBR材质（基础色、金属度、粗糙度、法线贴图）
  - 支持材质参数实时修改（如皮肤切换时替换基础色贴图）
  - 支持材质实例化，避免重复创建材质对象占用内存
- •模型部件管理：
  - 支持模型部件单独控制（如武器切换、特效挂载点显示/隐藏）
  - 支持部件碰撞盒独立配置，提升碰撞检测精度

------

## 📜 四、配置文件规范（生产级增强版）

### 1. 全局设置 (`settings.yaml`)

```yaml
# 游戏全局设置
language: "zh"
resolution: [1280, 720]  # 默认分辨率
resolution_options: [ [960, 540], [1280, 720], [1920, 1080] ]  # 可选分辨率
fps: 60  # 目标帧率
fullscreen: false  # 是否默认全屏

# 皮肤系统设置
skin_system:
  enabled: true
  default_skin: "basic"
  unlock_animation: true
  preview_enabled: true
  preview_type: "3d"  # 皮肤预览类型：2d/sprite/3d

# 技能效果系统设置
effect_system:
  max_effects_per_entity: 10
  effect_duration_scale: 1.0
  immunity_duration: 5.0
  particle_layer_count: 3  # 粒子分层数量（前景/中景/背景）
  max_particles: 500  # 全局最大粒子数量（性能控制）

# 渲染系统设置（新增）
render_system:
  render_pipeline: "forward"  # 渲染管线：forward/deferred
  anti_aliasing: "fxaa"  # 抗锯齿算法：none/fxaa/taa
  anti_aliasing_quality: "medium"  # 抗锯齿质量：low/medium/high
  shadow_quality: "medium"  # 阴影质量：none/low/medium/high
  shadow_resolution: 1024  # 阴影贴图分辨率
  ambient_occlusion: false  # 是否开启环境光遮蔽
  bloom_enabled: true  # 是否开启Bloom效果
  bloom_intensity: 0.8  # Bloom强度
  texture_quality: "high"  # 纹理质量：low/medium/high
  texture_anisotropy: 4  # 各向异性过滤等级

# 打击感系统设置（新增）
hit_feedback_system:
  enabled: true
  vibration_intensity: 80  # 振动强度（0-100）
  screen_shake_intensity: 0.1  # 屏幕抖动强度（0-1）
  damage_text_duration: 1.5  # 伤害数值显示时长（秒）
  crit_text_scale: 1.5  # 暴击数值缩放比例
  hit_sound_volume: 1.0  # 打击音效音量（相对于全局音量）

# 性能监控设置（新增）
performance_system:
  enabled: true
  monitor_interval: 0.5  # 监控采样间隔（秒）
  fps_warning_threshold: 30  # 帧率警告阈值（FPS）
  fps_danger_threshold: 20  # 帧率危险阈值（FPS）
  memory_warning_threshold: 0.6  # 内存警告阈值（占可用内存比例）
  auto_adjust: true  # 是否开启自动性能调整
  adjust_delay: 3.0  # 性能调整延迟（秒，避免频繁调整）

# 管理界面设置（仅开发人员可见）
admin:
  enabled: true
  password: "admin"  # 实际应使用哈希存储
  shortcut: "ctrl+shift+d"
  recycle_bin_path: "admin/recycle_bin"
  log_path: "admin/admin_log.txt"
  asset_upload:
    max_image_size: 2048  # 图片最大尺寸（像素）
    max_audio_size: 5242880  # 音频最大大小（5MB）
    max_model_size: 52428800  # 模型最大大小（50MB）
    max_material_size: 10485760  # 材质最大大小（10MB）
    allowed_formats:  # 允许的格式
      image: [".png", ".jpg", ".jpeg"]
      audio: [".wav", ".ogg", ".mp3"]
      video: [".mp4", ".avi"]
      model: [".fbx", ".glb"]
      material: [".mat", ".json"]
      shader: [".glsl", ".hlsl"]
```

### 2. 游戏元信息 (`games/<game>/meta.yaml`)

```yaml
name: "地下城与勇士"
type: defender
version: "1.0"
author: "社区贡献者"
icon: "assets/icons/dnf.png"
description: "经典横版格斗游戏，以爽快打击感著称"
supported_effect_types:  # 支持的效果类型
  - damage
  - heal
  - slow
  - stun
  - bleed
  - burn
# 游戏专属渲染配置（新增）
render_config:
  shader_pack: "dnf_shaders"  # 专属着色器包路径
  default_shadow_type: "soft"  # 默认阴影类型：hard/soft
  ambient_light_color: [0.8, 0.8, 0.8]  # 环境光颜色（RGB）
  ambient_light_intensity: 0.6  # 环境光强度
# 游戏专属性能配置（新增）
performance_config:
  model_lod_distance: [500, 1000, 1500]  # LOD切换距离（近/中/远）
  particle_limit: 300  # 该游戏IP的最大粒子数量
  animation_fps: 30  # 骨骼动画播放帧率
```

### 3. 角色定义 (`games/<game>/characters/<char>.yaml`)

```yaml
character_id: "zhaohuan"
name: "召唤师"
type: defender
cost: 100
description: "擅长召唤各种契约生物作战，具备多种控制与持续伤害技能"

# 基础属性
stats:
  hp: 300
  attack: 20
  attack_range: 250
  attack_speed: 1.5
  speed: 0
  crit_rate: 0.05  # 暴击率（0-1）
  crit_damage: 1.5  # 暴击伤害倍数
  effect_resistance: 0.1  # 效果抵抗率（0-1）

# 技能列表
skills:
  - skill_id: "summon_ice_cat"
    name: "召唤冰猫"
    type: summon
    cooldown: 10.0
    description: "召唤一只冰猫协助作战，冰猫攻击附带减速效果"
    
    # 伤害配置
    damage:
      base_damage: 30
      damage_type: "magic"  # physical/magic/true
      is_multi_hit: false
      crit_chance: 0.05
    
    # 效果配置
    effects:
      - effect_id: "ice_slow"
        chance: 1.0
        duration: 3.0
        potency: 0.3
    
    # 打击反馈配置（新增）
    hit_feedback:
      type: "light"  # 反馈类型：light/heavy/crit/heal/control
      sound: "assets/sounds/summon_attack.wav"
      vibration_duration: 0.1  # 振动时长（秒）
      vibration_strength: 30  # 振动强度（0-100）
      damage_text_color: [1.0, 1.0, 1.0]  # 伤害数值颜色（RGB）
    
    # 资源引用
    assets:
      icon: "assets/skills/ice_trap_icon.png"
      animation: "assets/animations/ice_trap.json"
      sound_cast: "assets/sounds/ice_trap_cast.wav"
      sound_hit: "assets/sounds/ice_trap_hit.wav"
      particle: "assets/effects/ice_summon.json"

  - skill_id: "ice_trap"
    name: "冰封陷阱"
    type: aoe
    cooldown: 15.0
    description: "放置一个冰封陷阱，对范围内敌人造成伤害和减速"
    
    # 伤害配置
    damage:
      base_damage: 50
      damage_type: "magic"
      is_multi_hit: true
      hit_times: 3
      interval: 0.5
      crit_chance: 0.1
    
    # 效果配置
    effects:
      - effect_id: "ice_slow"
        chance: 1.0
        duration: 3.0
        potency: 0.3
        
      - effect_id: "chill"
        chance: 0.5
        duration: 2.0
    
    # 区域效果
    area:
      radius: 100
      shape: "circle"  # circle/rectangle/conic
      
    # 打击反馈配置（新增）
    hit_feedback:
      type: "heavy"
      sound: "assets/sounds/ice_explode.wav"
      vibration_duration: 0.2
      vibration_strength: 60
      damage_text_color: [0.0, 0.75, 1.0]
      screen_shake: 0.05  # 屏幕抖动强度
    
    # 资源引用
    assets:
      icon: "assets/skills/ice_trap_icon.png"
      animation: "assets/animations/ice_trap.json"
      sound_cast: "assets/sounds/ice_trap_cast.wav"
      sound_hit: "assets/sounds/ice_trap_hit.wav"
      particle: "assets/effects/ice_explode.json"

# 可用皮肤列表
available_skins:
  - "basic"           # 基础皮肤
  - "ice_summoner"    # 冰晶召唤师
  - "fire_summoner"   # 火焰召唤师

# 模型配置（新增）
model_config:
  base_model: "models/characters/summoner.glb"  # 基础模型路径
  lod_levels: 3  # LOD层级数量
  lod_distances: [300, 600, 1000]  # LOD切换距离（像素）
  default_animation: "idle"  # 默认动画
  animations:  # 动画映射
    idle: "animations/summoner_idle.glb"
    attack: "animations/summoner_attack.glb"
    hit: "animations/summoner_hit.glb"
    death: "animations/summoner_death.glb"
  collision_box:  # 碰撞盒配置（新增）
    type: "aabb"  # 碰撞盒类型：aabb/obb/sphere
    size: [50, 100, 50]  # 碰撞盒尺寸（长/宽/高）
    offset: [0, 50, 0]  # 碰撞盒偏移量

# 武器配置（新增）
weapon_config:
  equipped_weapon: "staff"  # 默认装备武器
  weapons:
    staff:
      model: "models/weapons/magic_staff.glb"
      material: "materials/weapons/magic_staff.mat"
      animation: "animations/staff_attack.glb"
      attack_range_offset: 50  # 攻击范围偏移（延长攻击距离）

# 资源引用
assets:
  basic_sprite: "assets/sprites/summoner.png"
  basic_icon: "assets/icons/summoner.png"
  sound_attack: "assets/sounds/attack.wav"
  sound_death: "assets/sounds/death.wav"
  material: "materials/characters/summoner.mat"  # 基础材质（新增）

# AI行为配置
ai:
  behavior_tree: 
    - condition: "distance < 100"
      action: "use_skill summon_ice_cat"
      priority: 1
    - condition: "distance >= 100"
      action: "use_basic_attack"
      priority: 2

# 自定义逻辑脚本（可选）
custom_logic: "scripts/ai_summoner.py"

# 性能优化配置（新增）
performance_config:
  particle_limit: 50  # 该角色的最大粒子数量
  animation_optimization: true  # 是否开启动画优化
  lod_auto_switch: true  # 是否自动切换LOD
  distance_cull: true  # 是否开启距离剔除（超远时隐藏）
  distance_cull_threshold: 2000  # 距离剔除阈值（像素）
```

### 4. 皮肤定义 (`games/<game>/skins/<skin>.yaml`)（增强）

```yaml
skin_id: "ice_summoner"
name: "冰晶召唤师"
character_id: "zhaohuan"
description: "寒冰之力加持的召唤师，技能特效与模型均带有冰晶质感"
author: "设计团队"
version: "1.0"

# 解锁条件
unlock_conditions:
  type: "level"  # level/achievement/purchase
  value: 10
  price: 1000  # 如果是购买类型

# 属性加成（可选，叠加到基础属性）
stat_bonus:
  hp: 50
  attack: 10
  attack_speed: 0.1
  ice_resistance: 0.2

# 特效加成（新增）
effect_bonus:
  ice_skills:
    damage_multiplier: 1.2
    slow_duration: 2.0
  movement:
    trail_effect: "assets/effects/ice_trail.json"

# 模型与材质配置（新增）
model_config:
  override_model: "models/characters/summoner_ice.glb"  # 覆盖基础模型（可选）
  override_animations:  # 覆盖动画（可选）
    idle: "animations/summoner_ice_idle.glb"
    attack: "animations/summoner_ice_attack.glb"
  lod_distances: [350, 700, 1100]  # 皮肤专属LOD距离（可选）
  weapon_override:  # 武器覆盖（可选）
    staff:
      model: "models/weapons/ice_staff.glb"
      material: "materials/weapons/ice_staff.mat"

# 材质配置（新增）
material_config:
  override_material: "materials/characters/summoner_ice.mat"  # 覆盖基础材质
  material_parameters:  # 材质参数调整（可选）
    metallic: 0.3  # 金属度
    roughness: 0.5  # 粗糙度
    emission_color: [0.0, 0.5, 1.0]  # 自发光颜色
    emission_intensity: 0.8  # 自发光强度

# 专属资源
assets:
  sprite: "assets/skins/ice_summoner.png"
  icon: "assets/skins/ice_summoner_icon.png"
  select_animation: "assets/animations/ice_summoner_select.json"
  skill_effects:
    summon_ice_cat: "assets/skills/ice_cat_enhanced.json"
    ice_trap: "assets/skills/ice_trap_enhanced.json"
  sounds:
    attack: "assets/sounds/ice_attack.wav"
    skill: "assets/sounds/ice_skill.wav"
    death: "assets/sounds/ice_death.wav"
  particle_effects:  # 皮肤专属粒子特效（新增）
    trail: "assets/effects/ice_trail.json"
    idle_aura: "assets/effects/ice_aura.json"

# 皮肤专属技能调整（可选）
skill_adjustments:
  summon_ice_cat:
    cooldown: -1.0  # 减少冷却1秒
    effect_bonus: 
      damage: 10
      slow_potency: 0.2
  ice_trap:
    hit_feedback:  # 技能打击反馈调整（新增）
      type: "heavy"
      sound: "assets/sounds/ice_trap_skin_hit.wav"
      vibration_strength: 70

# 画质适配配置（新增）
graphics_adaptation:
  high_end:  # 高端设备配置
    enable_emission: true
    enable_advanced_particles: true
    particle_count_multiplier: 1.5
  mid_end:  # 中端设备配置
    enable_emission: true
    enable_advanced_particles: true
    particle_count_multiplier: 1.0
  low_end:  # 低端设备配置
    enable_emission: false
    enable_advanced_particles: false
    particle_count_multiplier: 0.5
    disable_lod_override: true  # 禁用皮肤专属LOD，使用基础角色LOD
```

### 5. 技能效果定义 (`games/<game>/effects/<effect>.yaml`)（增强）

```yaml
effect_id: "ice_slow"
name: "寒冰减速"
type: slow
description: "降低目标移动速度，附带冰晶粒子特效"

# 效果参数
parameters:
  slow_percentage: 0.3  # 减速30%
  duration: 3.0  # 持续时间3秒
  stack_type: "refresh"  # 叠加类型：refresh/stack/max
  stack_limit: 3  # 最大叠加层数（仅stack类型有效）

# 视觉效果（增强）
visual_effects:
  particle: "assets/effects/ice_slow.particle"
  particle_layer: "middle"  # 粒子渲染层级：foreground/middle/background
  particle_count: 20  # 粒子数量
  particle_lod: 2  # 粒子LOD等级
  icon: "assets/icons/ice_slow.png"
  icon_position: "top_right"  # 图标显示位置：top_left/top_right/bottom_left/bottom_right
  overlay_color: [0.0, 0.75, 1.0, 0.3]  # 目标叠加色（RGBA）
  overlay_duration: 0.5  # 叠加色显示时长
  sound: "assets/sounds/ice_slow.wav"
  sound_3d: true  # 是否为3D音效（随距离衰减）

# 状态指示器
status_indicator:
  color: [0, 191, 255]  # 冰蓝色
  icon: "assets/ui/slow_icon.png"
  pulse: true  # 是否脉冲闪烁
  pulse_interval: 1.0  # 脉冲间隔（秒）

# 免疫关系
immunity:
  cancels: ["speed_boost"]  # 取消速度增益效果
  blocked_by: ["ice_immunity"]  # 被冰免疫阻挡

# 性能配置（新增）
performance_config:
  particle_distance_cull: true  # 是否根据距离剔除粒子
  cull_distance: 800  # 剔除距离（像素）
  low_end_particle_count: 10  # 低端设备粒子数量
  disable_on_low_performance: false  # 低性能时是否禁用该效果
```

### 6. 模型配置定义 (`games/<game>/models/<model_type>/<model>.yaml`)（新增）

```yaml
model_id: "summoner"
name: "召唤师基础模型"
type: "character"  # 模型类型：character/weapon/prop/terrain
file_path: "models/characters/summoner.glb"  # 模型文件路径
format: "glb"  # 模型格式：fbx/glb
version: "1.0"
author: "建模团队"
description: "召唤师基础3D模型，含完整骨骼动画"

# 网格配置
mesh_config:
  triangle_count: 8000  # 三角面数量（高模）
  lod_levels: 3  # LOD层级数量
  lod_triangle_counts: [8000, 4000, 2000]  # 各LOD三角面数量
  collision_mesh: "models/characters/summoner_collision.glb"  # 碰撞网格（可选）

# 骨骼动画配置
animation_config:
  has_skeleton: true  # 是否包含骨骼
  bone_count: 32  # 骨骼数量
  animation_clips:  # 动画片段列表
    - name: "idle"
      duration: 2.0  # 动画时长（秒）
      loop: true  # 是否循环
      speed: 1.0  # 播放速度
    - name: "attack"
      duration: 0.8
      loop: false
      speed: 1.2
    - name: "hit"
      duration: 0.5
      loop: false
      speed: 1.0
    - name: "death"
      duration: 2.5
      loop: false
      speed: 1.0

# 材质关联
material_config:
  default_material: "materials/characters/summoner.mat"  # 默认材质
  material_slots:  # 材质插槽（多材质模型）
    - slot_name: "body"
      material: "materials/characters/summoner_body.mat"
    - slot_name: "clothes"
      material: "materials/characters/summoner_clothes.mat"
    - slot_name: "weapon"
      material: "materials/weapons/magic_staff.mat"

# 挂载点配置（新增，用于挂载特效/武器）
attachment_points:
  - name: "hand_r"  # 右手挂载点（武器）
    bone_name: "RightHand"  # 关联骨骼名称
    offset: [10, 0, 0]  # 挂载偏移
    rotation: [0, 0, 0]  # 挂载旋转
  - name: "head"  # 头部挂载点（头饰）
    bone_name: "Head"
    offset: [0, 20, 0]
    rotation: [0, 0, 0]
  - name: "back"  # 背部挂载点（翅膀/披风）
    bone_name: "Spine2"
    offset: [0, 0, -10]
    rotation: [0, 0, 0]

# 性能配置
performance_config:
  use_instancing: true  # 是否支持实例化渲染（批量创建时优化）
  auto_optimize_mesh: true  # 是否自动优化网格（移除冗余顶点）
  animation_optimization: true  # 是否优化动画（减少关键帧）
  low_end_simplify: true  # 低端设备是否自动简化模型
  simplify_triangle_ratio: 0.5  # 简化后三角面比例（相对于原模型）
```

### 7. 材质配置定义 (`games/<game>/materials/<material>.yaml`)（新增）

```yaml
material_id: "summoner_ice"
name: "冰晶召唤师材质"
type: "pbr"  # 材质类型：pbr/unlit/transparent
description: "适用于冰晶召唤师皮肤的PBR材质，带有冰质感和自发光"
shader: "shaders/standard_pbr.glsl"  # 关联着色器

# PBR材质参数（仅PBR类型有效）
pbr_parameters:
  base_color_texture: "textures/characters/summoner_ice_basecolor.png"  # 基础色贴图
  base_color: [1.0, 1.0, 1.0, 1.0]  # 基础色（叠加在贴图上）
  metallic_texture: "textures/characters/summoner_ice_metallic.png"  # 金属度贴图
  metallic: 0.3  # 金属度（0-1，无贴图时生效）
  roughness_texture: "textures/characters/summoner_ice_roughness.png"  # 粗糙度贴图
  roughness: 0.5  # 粗糙度（0-1，无贴图时生效）
  normal_texture: "textures/characters/summoner_ice_normal.png"  # 法线贴图
  normal_strength: 1.0  # 法线强度
  ao_texture: "textures/characters/summoner_ice_ao.png"  # 环境光遮蔽贴图
  ao_strength: 0.8  # AO强度
  emission_texture: "textures/characters/summoner_ice_emission.png"  # 自发光贴图
  emission_color: [0.0, 0.5, 1.0]  # 自发光颜色
  emission_intensity: 0.8  # 自发光强度（0-5）

# 透明材质参数（仅transparent类型有效）
transparent_parameters:
  alpha_mode: "blend"  # 透明模式：blend/mask
  alpha_cutoff: 0.5  # 透明度阈值（mask模式有效）

# 渲染配置
render_config:
  double_sided: false  # 是否双面渲染
  depth_write: true  # 是否写入深度缓冲
  depth_test: true  # 是否进行深度测试
  cull_mode: "back"  # 剔除模式：none/front/back
  render_queue: "opaque"  # 渲染队列：opaque/transparent/overlay

# 性能配置
performance_config:
  texture_compression: true  # 是否启用纹理压缩
  compression_quality: "medium"  # 压缩质量：low/medium/high
  mipmap_enabled: true  # 是否生成Mipmap（纹理缩放优化）
  anisotropic_filtering: true  # 是否启用各向异性过滤
```

### 8. 地图配置定义 (`campaigns/<campaign>/maps/<map>.yaml`)（新增）

```yaml
map_id: "dnf_vs_lol_level01"
name: "阿拉德峡谷"
description: "DNF与LOL对战的新手关卡地图，地形平坦，视野开阔"
type: "defense"  # 地图类型：defense/attack/arena
background_image: "maps/background.png"  # 2D背景图
terrain_model: "maps/terrain.glb"  # 3D地形模型（新增）
terrain_scale: [1.0, 1.0, 1.0]  # 地形缩放比例

# 路径配置
path_config:
  path_points: "maps/path_points.json"  # 路径点文件
  path_width: 80  # 路径宽度（像素）
  path_color: [0.5, 0.5, 0.5, 0.3]  # 路径显示颜色（编辑器用）

# 放置配置
placement_config:
  placement_mask: "maps/placement_mask.png"  # 放置掩码（白色可放置，黑色不可放置）
  placement_precision: "pixel"  # 放置精度：pixel/grid
  grid_size: [50, 50]  # 网格大小（grid精度有效）
  max_defenders: 20  # 最大防御方数量
  forbidden_areas:  # 禁止放置区域（新增）
    - type: "rectangle"
      position: [300, 200]
      size: [100, 100]
    - type: "circle"
      position: [600, 400]
      radius: 50

# 碰撞配置（新增）
collision_config:
  collision_mask: "maps/collision_mask.png"  # 碰撞掩码（白色可碰撞，黑色不可碰撞）
  collision_layer: "terrain"  # 碰撞层：terrain/obstacle/character
  obstacle_models:  # 障碍物模型（新增）
    - model: "models/props/rock.glb"
      position: [400, 300, 0]
      rotation: [0, 0, 0]
      scale: [1.0, 1.0, 1.0]
      collision_box: [30, 60, 30]

# 光影配置（新增）
lighting_config:
  ambient_light:
    color: [0.9, 0.9, 0.9]  # 环境光颜色
    intensity: 0.7  # 环境光强度
  directional_lights:  # 方向光列表
    - color: [1.0, 0.95, 0.8]
      direction: [-1.0, -1.0, -1.0]  # 光照方向
      intensity: 1.0
      cast_shadows: true  # 是否投射阴影
      shadow_resolution: 1024
  point_lights:  # 点光源列表
    - color: [1.0, 0.5, 0.5]
      position: [500, 200, 100]
      intensity: 2.0
      range: 300  # 光照范围
      cast_shadows: false
  spot_lights:  # 聚光灯列表（新增）
    - color: [0.5, 0.5, 1.0]
      position: [700, 400, 150]
      direction: [-1.0, 0, 0]
      intensity: 1.5
      range: 400
      inner_angle: 30  # 内锥角（度）
      outer_angle: 60  # 外锥角（度）
      cast_shadows: false

# 环境效果配置（新增）
environment_config:
  fog_enabled: true  # 是否开启雾效
  fog_color: [0.8, 0.9, 1.0]  # 雾色
  fog_start: 1000  # 雾开始距离
  fog_end: 2000  # 雾结束距离
  fog_density: 0.001  # 雾浓度（指数雾有效）
  weather_effect: "rain"  # 天气效果：none/rain/snow/wind
  weather_intensity: 0.5  # 天气强度
  weather_speed: 1.0  # 天气粒子移动速度

# 性能配置（新增）
performance_config:
  terrain_lod: true  # 是否启用地形LOD
  terrain_lod_distances: [500, 1000, 1500]  # 地形LOD切换距离
  obstacle_culling: true  # 是否启用障碍物距离剔除
  culling_distance: 1200  # 剔除距离
  particle_limit: 200  # 地图环境粒子最大数量
```

### 9. 战役定义 (`campaigns/<id>/campaign.yaml`)（增强）

```yaml
campaign_id: "dnf_vs_lol"
name: "阿拉德 vs 峡谷"
description: "DNF角色对抗LOL英雄！体验横版格斗与MOBA英雄的跨界碰撞"
cover_image: "cover.jpg"
cover_image_3d: "models/covers/dnf_vs_lol.glb"  # 3D封面模型（新增）
defender_game: "dnf"
attacker_game: "league_of_legends"
difficulty_levels: ["easy", "normal", "hard"]

# 皮肤解锁配置
skin_unlocks:
  - skin_id: "ice_summoner"
    unlock_condition: "complete_campaign"
    requirement: "dnf_vs_lol"

# 解锁条件
unlock_conditions:
  - complete: "lol_vs_genshin/level_01"

# 全局光影配置（新增）
global_lighting:
  ambient_light_color: [0.85, 0.85, 0.85]
  ambient_light_intensity: 0.6
  directional_light_color: [1.0, 0.95, 0.8]
  directional_light_intensity: 1.2
  shadow_quality: "medium"

# 性能配置（新增）
performance_config:
  default_device_profile: "mid_end"  # 默认设备性能配置文件
  max_entities: 50  # 战役最大实体数量（角色+技能+子弹）
  max_particles: 800  # 战役最大粒子数量
  frame_rate_target: 30  # 战役目标帧率
```

### 10. 关卡定义 (`campaigns/<id>/levels/level_xx.yaml`)（增强）

```yaml
level_id: "level_01"
name: "第一关：新手训练"
map_id: "dnf_vs_lol_level01"  # 关联地图配置（新增）
map_background: "maps/background.png"
placement_mask: "maps/placement_mask.png"
path_points: "maps/path_points.json"
collision_mask: "maps/collision_mask.png"  # 碰撞掩码（新增）

# 关卡特效环境（增强）
environment_effects:
  - effect_id: "raining"
    intensity: 0.7
    affects_all: true
    particle_count: 100
    cull_distance: 1000
  - effect_id: "night_vision"
    required: true
    post_process: "assets/post_process/night_vision.json"  # 后处理效果（新增）

# 关卡光影配置（新增，覆盖战役全局配置）
lighting_config:
  ambient_light_intensity: 0.5
  directional_light_direction: [-0.5, -1.0, -0.5]
  point_lights:
    - position: [300, 150, 50]
      color: [1.0, 0.2, 0.2]
      intensity: 1.5
      range: 200

# 波次配置
waves:
  - wave_id: "wave_1"
    time: 5.0
    enemies:
      - character: "garen"
        count: 3
        interval: 1.0
        health_multiplier: 1.0
        skin_override: "steel_legion_garen"  # 敌人皮肤配置
        model_scale: 1.0  # 模型缩放（新增）
        animation_speed: 1.0  # 动画速度（新增）
        
  - wave_id: "boss_wave"
    time: 20.0
    enemies:
      - character: "lux"
        count: 2
        interval: 2.0
        health_multiplier: 1.2
        skin_override: "elementalist_lux"
        model_scale: 1.1
        attack_range_multiplier: 1.2  # 攻击范围倍率（新增）

# Boss配置（增强）
boss:
  character: "teemo"
  health_multiplier: 2.0
  skin: "devil_teemo"  # Boss专属皮肤
  model_scale: 1.5  # Boss模型放大
  animation_speed: 0.9  # 动画速度减慢
  drop_items: ["rune", "coin"]
  special_effects:  # Boss专属环境特效（新增）
    - effect_id: "poison_aura"
      radius: 200
      intensity: 1.0
      duration: -1  # 永久生效
  hit_feedback:  # Boss打击反馈（新增）
    normal_hit:
      type: "heavy"
      vibration_duration: 0.3
      screen_shake: 0.1
    crit_hit:
      type: "crit"
      vibration_duration: 0.5
      screen_shake: 0.2
      sound: "assets/sounds/boss_crit_hit.wav"

# 胜利/失败条件
victory_conditions:
  - base_hp > 0
  - time > 300

defeat_conditions:
  - base_hp <= 0

# 性能配置（新增）
performance_config:
  wave_particle_limit: 300  # 单波次最大粒子数量
  boss_fight_particle_limit: 500  # Boss战最大粒子数量
  low_end_disable_effects: ["raining", "poison_aura"]  # 低端设备禁用的效果
  dynamic_quality_adjust: true  # 战斗中动态调整画质
```

### 11. 性能配置文件 (`performance/device_profiles/high_end.yaml`)（新增）

```yaml
profile_id: "high_end"
name: "高端设备配置"
description: "适用于旗舰手机/高性能PC的性能配置，全开画质特效"
device_requirements:  # 设备性能要求（用于自动识别）
  cpu_cores: 8+
  ram: 6GB+
  gpu: "adreno650+/mali-g78+/gtx1050+"

# 渲染性能配置
render_config:
  render_pipeline: "deferred"
  anti_aliasing: "taa"
  anti_aliasing_quality: "high"
  shadow_quality: "high"
  shadow_resolution: 2048
  ambient_occlusion: true
  bloom_enabled: true
  bloom_intensity: 1.0
  texture_quality: "high"
  texture_anisotropy: 8
  post_processing: ["bloom", "hdr", "color_correction"]  # 启用的后处理效果

# 模型性能配置
model_config:
  lod_levels: 3
  lod_distance_multiplier: 1.2  # LOD切换距离倍率（更远才切换低模）
  model_detail: "high"  # 模型细节：high/medium/low
  animation_quality: "high"  # 动画质量：high（完整关键帧）/low（简化关键帧）
  instance_rendering: true

# 粒子性能配置
particle_config:
  max_particles: 1000
  particle_quality: "high"
  particle_lod: 3
  advanced_particles: true  # 启用高级粒子（如体积雾、流体效果）

# 实体性能配置
entity_config:
  max_entities: 80
  entity_update_rate: 60  # 实体更新帧率
  distance_cull_threshold: 2500  # 实体剔除距离

# 资源加载配置
loading_config:
  preload_distance: 1500  # 预加载距离
  cache_size: 2GB  # 资源缓存大小
  texture_compression: false  # 禁用纹理压缩（保留画质）
```

------

## 🖼️ 五、UI 与交互要求（生产级增强版）

### 1. **战役选择界面**（增强）

- •动态网格布局（支持缩放，适配不同分辨率）
- •每个战役卡片显示：
  - 3D封面模型（可旋转预览，鼠标悬停时播放动画）
  - 名称/描述（支持换行，自适应卡片宽度）
  - 解锁状态（锁/星阶评分）
  - 难度标签（easy/normal/hard，颜色区分：绿/黄/红）
  - 推荐配置（适配设备性能的画质等级提示）
- •交互效果：
  - 鼠标悬停：卡片上浮、阴影加深、3D模型轻微旋转
  - 点击：播放选中动画、弹出战役详情面板（含关卡列表、奖励预览）
  - 支持搜索/筛选（按难度、IP、解锁状态）

### 2. **角色选择界面**（生产级增强）

- •皮肤选择器 ：
  - 3D模型预览窗口（支持旋转/缩放/播放动画）
  - 皮肤属性对比面板（基础属性+特效加成+性能消耗）
  - 解锁状态提示（锁图标+解锁条件文字）
  - 皮肤特效预览按钮（播放技能特效/待机特效演示）
  - 性能消耗标识（高/中/低，提示该皮肤对设备的要求）
- •技能展示 ：
  - 技能图标（带冷却时间预览）
  - 技能效果演示（GIF/短视频，点击播放全屏演示）
  - 连招提示和推荐（基于角色技能组合）
  - 技能打击感预览（播放打击音效+振动反馈演示）
- •角色详情面板 ：
  - 3D模型+2D立绘切换显示
  - 基础属性条形图（直观对比）
  - 技能树/技能组合说明
  - 适用关卡/克制关系提示
  - 玩家使用评分（社区反馈）

### 3. **战斗界面交互**（生产级增强）

- •左侧面板 ：
  - 角色图标（带冷却进度条、血量百分比、状态图标）
  - 资源显示（金币/阳光，带获取动画）
  - 技能快捷栏（拖拽放置，支持长按查看技能详情）
  - 皮肤快速切换按钮（战斗间隙切换，需CD）
- •中部战场 ：
  - 9x5网格（可配置，支持自定义网格大小）
  - 放置提示（高亮可放置区域，红色标注不可放置区域）
  - 实时血条显示（角色/基地，血条颜色随血量变化：绿→黄→红）
  - 模型实时动画（攻击/受伤/移动动画流畅播放）
  - 技能特效分层渲染（前景技能不遮挡角色，背景特效烘托氛围）
  - 打击反馈可视化（数值飘出、屏幕轻微抖动、命中闪光）
- •状态效果栏 （增强）：
  - 显示当前生效的buff/debuff图标（带剩余时间倒计时）
  - 图标大小随效果重要性变化（控制效果>伤害效果


## 🖼️ 六、交付标准（生产级增强版）
> **终极验证六原则**：
>
> 1. 1.无需修改一行代码 → 新增游戏IP/角色/技能/皮肤/效果
> 2. 2.无需重启游戏 → 配置立即生效
> 3. 3.无需手动操作 → 所有文件自动生成
> 4. 4.无需担心错误 → 配置验证 + 回收站 + 日志
> 5. 5.复杂技能效果系统 → 多段伤害/控制效果/状态叠加/技能扩展
> 6. 6.完整皮肤系统 → 多皮肤切换/特效叠加/属性加成

------

> **开发人员使用指南**（见 `README.md`）
>
> - •**快捷键**：`Ctrl+Shift+D`
> - •**默认密码**：`admin`（请务必修改）
> - •**严禁手动编辑** `games/`或 `campaigns/`或 `skins/`目录
> - •**所有操作走管理界面**
> - •**删除 = 进回收站 ≠ 永久丢失**
> - •**效果系统调试工具**：F5打开效果调试面板
> - •**日志位置**：`admin/admin_log.txt`

------

✅ 此文档为最终权威版本，适用于开发、测试与部署全流程，包含完整的皮肤系统和技能效果系统。